import { Console } from '@/components/console/console';

// Flagship: the live guardrail console. Client component — it owns the socket stream.
export default function ConsolePage() {
  return <Console />;
}
