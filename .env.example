# --- Agent LLM (OpenAI) ---
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o

# --- Postgres (audit log + policy rules) ---
DATABASE_URL=postgresql://armoriq:armoriq@localhost:5432/armoriq?schema=public

# --- Server ---
PORT=4000

# Path to the BUILT security-ops MCP server (spawned over stdio by the registry).
# Build it first: npm run build -w packages/mcp-servers/security-ops
SECURITY_OPS_PATH=packages/mcp-servers/security-ops/dist/index.js
